---
name: notes-revision-2026-08-30
description: 2026-08-30 슬라이드 노트 대본 일괄 윤문. 무엇을 고쳤고 무엇을 일부러 남겼는지
metadata:
  type: project
---

사용자 지시(2026-08-30): "노트 대본이 전반적으로 어색한 한국어이고 과장된 수식어가 많다. 01-08 덱 전체를 검토·수정."

**대상**: `quarto/agent/2026-08-26/*.qmd` 의 `::: notes` 블록 **211개**(01장의 주석 처리된 1개 제외 — [[ch01-summary-slide-commented-out]]). 본문은 한 글자도 건드리지 않았고 스크립트가 그것을 강제한다.

**실측으로 좁힌 진짜 결함** (처음 눈에 띈 "습니다 92%"는 원인이 아니었다. 어미 끝단어 분포는 최빈값이 4.6%로 긴 꼬리였고 문장 길이도 p10~p90이 14~52자로 고르게 퍼져 있었다):

1. **같은 최상급의 반복** — "가장 중요한/제일 강조하고 싶은"이 슬라이드마다 등장(6곳). 여러 개가 동시에 가장 중요할 수는 없다.
2. **평가어 남용** — 재미있습니다, 흥미롭습니다, 참 쉽습니다, 압도적으로, 대단히, 몹시, 확, 아주, 완벽하게, 3초 만에
3. **폐기했던 `죠` 계열 잔존** — 되지요, 보이겠지요, 처방이지요 등 6건(2026-08-23에 238건을 0으로 만들었으나 재유입)
4. **문장 중복** — 03장 IDX 2의 두 문장("곱셈 하나가 …판정해 주는 셈입니다", "파란 플러스와 주황 마이너스가…")이 IDX 3에 그대로 재등장
5. **띄어쓰기·표기 오류** — "연구 질문 입니다"(+이중 공백), "데이터 입니다", "자료 중 하나 입니다", "년도라기 보다는" → 연도
6. **상투구 반복** — 떠올려 보십시오 10건, 생각하시면 됩니다 7건, 여기서 꼭 나오는 질문이 있습니다(2회)

**결과**: 72개 블록 수정. 과장 수식어 88 → **49**, AI 상투구 60 → **29**. 노트 수 211 유지, 본문 무변경, 8개 덱 렌더 exit 0.

**일부러 남긴 것**: `반드시`(21건 대부분이 "반드시 붙이십시오"처럼 정당한 당위), `진짜`(참값 대비 추정값을 가리키는 용법), `훨씬`(실제 배수 차이를 말하는 자리), 그리고 장별 덱은 독립 발표물이므로 **덱 사이의 비유 반복**(과녁-화살, 그물 던지기)은 그대로 두었다.

**남은 것 1건**: `03-simple-linear-regression.qmd:1145` 본문에 "압도적으로"가 있으나 **노트가 아니라 슬라이드 본문**이라 이번 요청 범위 밖으로 두었다.

**후속 수정(같은 날, 사용자 지적)**: 1차 수정에서 내가 만든 조어 5건을 다시 고쳤다. `지탱하는 한 문장`→`한 문장으로 나타낸다면 이렇게 말할 수 있습니다`, `이 강의 전체를 떠받칩니다`→`앞으로 계속 쓰일 식입니다`, `생활에서 바로 와닿는`→`실생활과 관련한`, `말이 그것입니다`→`말씀이 바로 그것입니다`, `실제 소득입니다`→`더 중요합니다`. 규칙은 [[slide-register-rule]]에 추가했다. 01·02·06 재렌더 exit 0.

**도구** (`code/agent/2026-08-30/`): `notes_lib.py`(공용 파서), `diagnose-notes.py`(코퍼스·지표 추출), `revise-notes.py`(`splice`/`verify`). verify는 ① 노트 밖 본문 바이트 동일 ② 노트 개수 동일 ③ 원문의 모든 숫자·수식 토큰이 수정본에 남아 있음 ④ 수정문에 div 펜스 없음을 검사한다. 의도적으로 지운 수사적 숫자는 `output/agent/2026-08-30/allowed-drops.txt` 에 사유와 함께 적는다 — 검사를 느슨하게 하지 말 것.

관련: [[slide-register-rule]], [[slide-level-feedback]], [[chapter-split-decks]]
