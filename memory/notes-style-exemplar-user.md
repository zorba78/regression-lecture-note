---
name: notes-style-exemplar-user
description: 노트 대본의 문체 기준은 사용자가 직접 쓴 01장 IDX 0·1이다. 그 특징과 적용 범위
metadata:
  type: feedback
---

**기준 원본**: `quarto/agent/2026-08-26/01-introduction.qmd` 의 `::: notes` **IDX 0(도입부)·1(회귀분석이란)·2(회귀의 의미)·3(Galton과 회귀의 기원)**. 사용자가 2026-08-30 직접 작성했다. 앞으로 노트 대본은 **이 네 블록의 톤과 용어를 기준**으로 쓴다. 이 블록들은 손대지 않는다([[respect-user-edits]]).

**문체 특징(실제 문장에서 추출)**

| 요소 | 사용자 문체 |
|:--|:--|
| 청중 관계 | 연구 경험을 인정하고 질문을 던짐 — "이미 회귀분석을 수행한 경험이 모두 있으실 것", "~사용하고 계신가요?" |
| 도입 | 개념 설명 전에 **질문 → 상식적 답 → 개념** 순서 |
| 예시 | 구체적 수치 — "대한민국 사람의 키 170cm", "아버지의 키가 180cm" |
| 문장 길이 | **짧게 끊지 않고 길게 이어 씀.** 연결어미로 문장을 잇는다 |
| 요약 | "**즉,** ~입니다"로 문단을 닫음 |
| 메타 코멘트 | 강의 운영·자료 선정 경위를 솔직히 밝힘 — "2-3시간 동안 모두 다루기는 어렵기 때문에", "인터넷 검색을 통해 골라서 가져왔습니다" |
| 인칭 | "우리는", "여러분들" |
| 문단 | 빈 줄로 2~4개 문단 구분 |
| 용어 | "측정 및 관찰에서 발생할 수 있는 오차", "무게중심", "특정 조건이 주어졌을 때 결과의 평균" |

**중요 — 내 이전 정비 기준과 충돌하는 부분**: 사용자는 `매우`, `굉장히`, `널리`, `즉,` 을 자유롭게 쓴다. 2026-08-30 1차 윤문에서 내가 이것들을 "과장 수식어·AI 상투구"로 보고 걷어냈는데 **사용자 본인 문체에서는 정상 어휘**다. 앞으로 이 계열을 기계적으로 제거하지 말 것. 관련: [[slide-register-rule]], [[notes-revision-2026-08-30]]

**적용 현황(2026-08-30)**: **01장·02장** 재작성 완료. 01장은 사용자 지시로 **IDX 4~21(18개 블록)**, 02장은 **IDX 0~27(28개 블록 전부)**를 이 톤으로 다시 썼다. 방법은 `code/agent/2026-08-30/revise-notes.py splice`(마커 `<<<DECK <파일명> IDX n>>>`)이고, 검증은 splice 전 스냅숏과 대조해 ① 노트 밖 본문 바이트 동일 ② 노트 블록 수 유지 ③ 사용자 작성 블록 무변경(01장 IDX 0~3) ④ 숫자·수식 토큰 유지를 확인했다(`revise-notes.py verify`는 본문·수치가 이미 바뀐 뒤라 HEAD 대조가 실패하므로 쓰지 않는다). 이어서 사용자 지시로 **03~08장 전 블록**도 같은 톤으로 재작성했다 — 03장 39개, 04장 43개, 05장 31개, 06장 25개, 07장 16개, 08장 7개(총 161개). 검증은 위와 같은 방식(splice 전 스냅숏 대조)으로 여섯 장 모두 ① 본문 동일 ② 블록 수 유지 ③ 숫자·수식 토큰 누락 0을 확인했고, 렌더 후 `slidecheck.py --deck NN --fresh`도 여섯 장 모두 clipped 0 / overflow 0이다. **이로써 01~08장 전 노트가 사용자 문체로 통일되었다.**

**02장 추가 지시(2026-08-30)**: 이론(행렬대수·확률분포)이 많은 장은 **12살이 이해할 수준의 예시**를 넣으라는 요구가 붙었다. 그래서 개념마다 일상 비유를 하나씩 달았다 — 전치=표를 대각선으로 뒤집기·양말과 신발, 행렬식=평행사변형 넓이가 0으로 눌림, 계수=cm칸과 mm칸(정보는 하나), 역행렬=되돌리기 버튼, 고유벡터=회전목마의 축, 스펙트럴=프리즘, 멱등=흑백 사진·그림자, 자유도=평균을 알면 마지막 한 사람은 자동으로 정해짐, 표집분포=내일 다시 실험하면, 신뢰구간=그물 던지기, 가설검정=형사재판, p 오해=불 꺼진 방. 슬라이드 본문의 수치는 그대로 유지하되 `x11·x12·x13` 같은 첨자 표기는 변수 이름으로 풀어 읽었다.
